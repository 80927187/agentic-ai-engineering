[project]
name = "lesson-agent-loop"
version = "0.1.0"
description = "第 5 课：智能体循环——反复使用工具的自主智能体"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "common",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "rich>=14.0.0",
]

[tool.uv.sources]
common = { path = "../../common", editable = true }

[tool.ruff]
line-length = 100
target-version = "py311"
