[project]
name = "lesson-evals"
version = "0.1.0"
description = "课程：评估——黄金数据集、LLM 充当评委和自动评分流水线"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "common",
    "python-dotenv>=1.0.0",
    "rich>=13.7.0",
]

[tool.uv.sources]
common = { path = "../../common", editable = true }

[tool.ruff]
line-length = 100
target-version = "py311"
